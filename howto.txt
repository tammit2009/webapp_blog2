# Flask Fridays (playlist)
# https://www.youtube.com/watch?v=0Qxtt4veJIc&list=PLCC34OHNcOtolz2Vd9ZSeSXWc8Bq23yEz&index=2

# Create a virtual environment
$ python -m venv virt 

# Activate the virtual environment
$ source virt/Scripts/activate 

# Deactivate whenever necessary
# $ deactivate 

# What is currently in our virtual environment
$ pip freeze

# Install required dependencies
$ pip install flask


# Create a simple web app

    # 'hello.py'
    from flask import Flask, render_template

    # Create a flask instance
    app = Flask(__name__)

    # Create a route decorator
    @app.route('/')
    def index():
        return "<h1>Hello World!</h1>"

# Setup run environment 
$ export FLASK_ENV=development
$ export FLASK_APP=hello
$ export FLASK_DEBUG=1

# Run the app 
$ flask run

# View web app on http://127.0.0.1:5000


Custom Error Pages and Version Control - Flask Fridays #3
https://www.youtube.com/watch?v=3O4ZmH5aolg&list=PLCC34OHNcOtolz2Vd9ZSeSXWc8Bq23yEz&index=3



# Create ssh key for version Control
$ cd ~/

$ pwd 
User@LAPTOP-3TUD7909 MINGW64 /c/Users/User

$ mkdir .ssh 

$ cd .ssh 

$ ssh-keygen.exe
$ ls 
id_rsa   id_rsa.pub

# Copy id_rsa.pub to github
$ cat id_rsa.pub 

# On Github, in settings, look for SSH Keys > Add new SSH Key

# Setup version control 
$ cd /c/Users/User/Dropbox/KustomDevOps/Tutorials/python/web/Flask/webapp_blog2

$ touch .gitignore

    # .gitignore
    .gitignore

    virt/
    
# Ensure to activate virtual environment
$ source virt/Scripts/activate

# Initialize git 
$ git config --global user.name "Tammi Takaya"
$ git config --global user.email "tb.takaya@gmail.com"
$ git config --global push.default matching
$ git config --global alias.co checkout
$ git init


